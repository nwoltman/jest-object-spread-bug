# jest-object-spread-bug

### Install

```sh
yarn install
```

### Run tests to see bug

```sh
yarn test
```

Note that tests will pass if either `collectCoverage` or `transform` are commented out in `jest.config.js`.
